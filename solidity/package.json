{
  "name": "usecontent-solidity",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy-subscription-manager.ts --network sepolia",
    "deploy:platform:sepolia": "hardhat run scripts/deploy-platform-subscription-manager.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.3",
    "@types/node": "^25.6.0",
    "hardhat": "^3.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
